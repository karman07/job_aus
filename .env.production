# Production Environment Variables
PORT=3001
NODE_ENV=production

# Database
MONGODB_URI=mongodb://localhost:27017/crossnations_backend
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/crossnations_backend

# JWT Secret (Generate a strong secret)
JWT_SECRET=your_super_secret_jwt_key_here_make_it_very_long_and_random

# Frontend URL (Update with your actual domain)
FRONTEND_URL=https://your-frontend-domain.com

# AWS SES (Already configured)
# No additional env vars needed - credentials are in code